packages:
  llvm:
    require:
      - "@18"
    externals:
    - spec: llvm@18
      prefix: $LLVM_INSTALL_DIR
  mpich:
    require:
      - "~fortran"
  all:
    require:
      - "%clang"
    providers:
      mpi: [mpich]

